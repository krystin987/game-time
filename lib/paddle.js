function Paddle(x, y, width, height, canvas, context) {
  this.x = x;
  this.y = y;
  this.width = width;
  this.height = height;
  this.canvas = canvas;
  this.context = context;
  // this.h = 15;
  // this.w = 200;
  // this.x = (canvas.width - this.w)/2;
  // this.y = canvas.height - this.h;
  // this.dX = 10;

  // this stuff was copied/pasted, it may not be the
  // heights etc we're looking for
};



module.exports = Paddle;
